# Build artifacts
dist/
.astro/
node_modules/

# Editor / OS
.DS_Store
*.tmp

# Local Claude config (per-user)
.claude/

# Secrets — never commit
.env
.env.*
!.env.example

# Legacy backups (kept locally, not for git)
*.bak
*.pdf.bak*
generation-gap_v2_7.html
