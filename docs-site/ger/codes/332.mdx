---
title: "GER-332 · Constraint Non-Propagation"
description: "A controller (orchestrator agent, supervisor team, parent process) operates under a constraint that is not propagated to subordinate agents it delegates to. Sub-agents execute without the constraint that would have preve…"
sidebarTitle: "332 · Constraint Non-Propagation"
---

import { Badge } from "/snippets/Badge.mdx"

<div className="flex flex-wrap gap-2 mb-8 mt-2">
<Badge tone="amber">Illustrative</Badge> <Badge>Tier 3xx</Badge> <Badge>Layer L2</Badge> <Badge tone="blue" title="SVRNOS-assigned code in an HTTP-unassigned slot">Namespace-Claim</Badge> <Badge tone="gray">Added v0.2</Badge>
</div>

*Structural Moves*

## Definition

A controller (orchestrator agent, supervisor team, parent process) operates under a constraint that is not propagated to subordinate agents it delegates to. Sub-agents execute without the constraint that would have prevented harm.

## Distinct from

- [**GER-337**](/ger/codes/337) — Constraints fail to propagate to sub-agents → this code. Non-constraint information (intent, context) attenuates across agents → GER-337.
- [**GER-347**](/ger/codes/347) — Constraint dropped during delegation → this code. Original intent mutated as each agent reinterprets → GER-347.

## Tags

`agentic` · `multi-agent`

### Cite

```
SVRNOS. (2026). GER-332: Constraint Non-Propagation. Governance Error Register. https://docs.svrnos.com/ger/codes/332
```
